import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  switch: {
    width: 45,
    height: 24,
    borderRadius: 208,
    padding: 4,
  },
  circle: {
    width: 16,
    height: 16,
    borderRadius: 8,
    backgroundColor: "#fff",
  },
});

export default styles;
